[package]
name = "gpui-pre-refineable"
version = "0.3.5"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct (gpui-pre snapshot of zed@d89e9c2)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "refineable"
zed-version = "0.1.0"
zed-rev = "d89e9c2124b2786a390c7a451c7488601b4da2e1"

[lib]
path = "src/refineable.rs"
doctest = false
name = "refineable"

[features]

[dependencies]
derive_refineable = { workspace = true }

[lints]
workspace = true
